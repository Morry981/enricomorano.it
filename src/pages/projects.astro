---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Progetti | Enrico Morano">
    <main class="max-w-6xl mx-auto px-8 py-20">
        <h1 class="text-5xl font-bold mb-12">I miei <span class="text-accent">Progetti</span></h1>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
            <!-- Placeholder per i futuri componenti Vue -->
            <div class="p-8 bg-primary/10 border border-primary/20 rounded-2xl h-64 flex items-center justify-center">
                <p class="text-secondary italic">Work in progress...</p>
            </div>
            <div class="p-8 bg-primary/10 border border-primary/20 rounded-2xl h-64 flex items-center justify-center">
                <p class="text-secondary italic">Work in progress...</p>
            </div>
        </div>
    </main>
</Layout>
